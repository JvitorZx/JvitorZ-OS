# Shared

O diretório `shared` contém componentes reutilizáveis e tipos comuns.

## Responsabilidades

- `utils/`: funções utilitárias e helpers genéricos
- `constants/`: constantes de aplicação e valores fixos
- `types/`: definições de tipos e interfaces comuns
